from th.modules import (CNNModel,
                        coatnet)

from th.utils import (get_loader as loader,
                      train_model as trainer,
                      evaluate_model as evaluator,
                      plot_metrics as plot)
